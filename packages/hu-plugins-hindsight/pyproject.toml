[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "hu-plugins-hindsight"
version = "0.1.0"
description = "HUAP plugin: Hindsight memory backend (retain/recall/reflect)"
requires-python = ">=3.10"
dependencies = [
    "huap-core>=0.1.0b1",
]

[project.optional-dependencies]
hindsight = ["hindsight-client"]
