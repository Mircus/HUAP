# Echo Pod Graph - Minimal example for testing
# This graph has two nodes: tool call and LLM greeting

nodes:
  - name: echo_tool
    run: examples.pods.echo_pod.nodes.echo_tool
  - name: echo_greet
    run: examples.pods.echo_pod.nodes.echo_greet

edges:
  - from: echo_tool
    to: echo_greet
  - from: echo_greet
    to: null
